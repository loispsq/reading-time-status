# build
main.js
node_modules/

# NOTE: package-lock.json is committed on purpose — Obsidian's build
# verification needs a lockfile to reproduce the build byte-for-byte.

# os / ide
.DS_Store
.vscode/

# obsidian data (若在 vault 内调试)
data.json
